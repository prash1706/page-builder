meta(charset="UTF-8")
meta(name="viewport", content="width=device-width, initial-scale=1")

link(rel="icon", href="//www.ibm.com/favicon.ico")
if meta.canonical
  link(rel="canonical", href="#{meta.canonical}")
if meta.keywords
  meta(name="keywords", content="#{meta.keywords}")
if meta.description
  meta(name="description", content="#{meta.description}")
if meta.industry
  meta(name="industry", content="#{meta.industry}")
if meta.robots
  meta(name="robots", content="#{meta.robots}")
if meta.dctDate
  meta(name="dcterms.date", content="#{meta.dctDate}")
if meta.dctRight
  meta(name="dcterms.rights", content="#{meta.dctRight}")


if meta.title
  title= meta.title
else
  title Northstar Mktg Landing Page Builder

script.
  digitalData = {
    page: {
      category: {
        primaryCategory: "#{meta.category}"
      },
      pageInfo: {
        effectiveDate: "#{meta.effectiveDate}",
        expiryDate: "#{meta.exprityDate}",
        language: "#{meta.language}",
        publishDate: "#{meta.publishDate}",
        publisher: "#{meta.publisher}",
        version: "v18",
        ibm: {
          contentDelivery: "#{meta.contentDelivery}",
          contentProducer: "#{meta.contentProducer}",
          country: "#{meta.country}",
          industry: "#{meta.industry}",
          owner: "#{meta.owner}",
          owningPortal: "#{meta.ownPortal}",
          siteID: "#{meta.siteID}",
          subject: "#{meta.subject}",
          type: "#{meta.type}"
        }
      }
    }
  };
script(src="//www.ibm.com/common/stats/ida_stats.js")
link(href="//1.www.s81c.com/common/v18/css/www.css",rel="stylesheet")
script(src="//1.www.s81c.com/common/v18/js/www.js")
script(src="//1.www.s81c.com/common/v18/js/greensock/TweenLite.min.js")
script(src="//cdnjs.cloudflare.com/ajax/libs/jquery-easing/1.3/jquery.easing.min.js")

link(href="http://webdev.somerslab.ibm.com/v18-portfolio/patterns/assets/fabricator/styles/f.css",rel="stylesheet")

script.
  IBMCore.common.util.config.set({
    masthead: { type: "alternate" },
    footer: {type: "alternate"}
  });
script.
   (function(h, o, t, j, a, r) {
     h.hj = h.hj || function() {
       (h.hj.q = h.hj.q || []).push(arguments)
     };
     h._hjSettings = {
       hjid: 42920,
       hjsv: 5
     };
     a = o.getElementsByTagName('head')[0];
     r = o.createElement('script');
     r.async = 1;
     r.src = t + h._hjSettings.hjid + j + h._hjSettings.hjsv;
     a.appendChild(r);
   })(window, document, '//static.hotjar.com/c/hotjar-', '.js?sv=');


